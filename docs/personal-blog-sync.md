# 개인 블로그 동기화

공개된 개인 네이버 블로그 `kjp9204`의 글을 정적 홈페이지로 옮긴다.
Hand 팀 글과 미공개 로컬 문서, 댓글, 프로필 데이터는 동기화 대상이 아니다.

## 2026-09-07 반영 범위

- 기존 GitHub 갱신: 2026-08-06 (기준 커밋 `50d87bf`).
- 이후 공개된 글 21편(2026-08-09~2026-09-07)을 RSS와 공개 글 목록으로 대조했다.
- 기존 개인 글 11편은 보존하여 개인 아카이브는 총 32편이다.
- 이는 해당 기간의 신규 글 동기화이며, 블로그의 모든 과거 글을 이관했다는 의미가 아니다.
- 원문의 문장·표·사진 순서를 보존했다. 네이버 갤러리 조작 버튼과 추적 스크립트는 제외했다.
- 네이버 및 외부 전용 영상은 원문 재생 링크로 제공한다. 내려받을 수 없는 외부 이미지는 명시적인 원문 확인 링크로 표시한다.
- 게시글의 주장·일정·가격을 새로 작성하거나 사실 검증한 작업은 아니다.

## 재실행

### 2026-09-17 반영 범위

- 기준 커밋: `bb0806d96c6cc1f5ff6519c98e1b0cc87f28068c`.
- 9월 7일 이후 공개된 신규 글 25편을 추가했다. 기존 개인 글 32편을 보존해 총 57편이다.
- RSS 최신 50편의 날짜 범위가 이전 동기화일까지 이어지는지 확인했다. 모든 과거 글의 완전 이관은 아니다.
- 게시글 ID, 정규화한 제목, 본문 해시와 본문 유사도를 대조했으며, 이번 아카이브 내 중복 후보는 없었다.
- 신규 글에 로컬 이미지 316개 배치, 내려받지 못한 외부 이미지 16개는 원문 확인 링크로 제공한다.
- 신규 글 25편의 전체 본문 원문 일치를 확인했다. 기존 21개 수입 글 중 6편은 현재 네이버 원문 끝에 작품/뉴스 정보 카드가 추가되어 과거 저장본과 다르다. 이 6편을 현재 원문 일치로 보고하지 않으며, 기존 저장본과 메타데이터는 기준 커밋과 동일하게 보존했다.
- 목록 미리보기는 신규 글 본문의 첫 긴 문단을 발췌하며, 본문 자체는 다시 쓰지 않는다.
- 미공개 초안, 내부 협업 기록 및 Hand 팀 콘텐츠는 이번 변경에 포함하지 않는다.

### 2026-09-25 반영 범위

- 기준 커밋: `e24f1367182487ce200928d6c597f1feeb8249a7`.
- 9월 18일~25일 공개된 새 글 26편을 추가했다. 기존 개인 글 57편을 보존해 총 83편이다.
- 이전 동기화 당일 늦게 게시된 글의 누락을 막기 위해 `--since 2026-09-16`으로 조회하고, 영구 게시글 ID로 이미 수입한 글을 제외했다. RSS 최신 50편이 이 시작일까지 포함하는지 확인했다.
- 게시글 ID, 정규화한 제목, 본문 해시와 7자 묶음 유사도(0.75 이상)를 대조했으며, 아카이브 내 중복 후보는 없었다.
- 새 글의 이미지 356개 배치를 로컬 자산으로 제공한다. 접근이 차단되거나 사라진 외부 이미지 6개는 원문 확인 링크로 표시했다.
- 새 글 26편의 전체 본문이 원문과 일치하고, 로컬 링크·메타데이터 및 중복 방지 회귀 검사 12건을 통과했다.
- 기존 개인 글 57편은 기준 커밋과 동일하다. 이전에 확인한 기존 수입 글 6편의 원문 변경은 별도로 보고했으며, 저장본을 덮어쓰지 않았다.
- 미공개 초안, 내부 협업 기록 및 Hand 팀 콘텐츠는 변경에 포함하지 않는다. 원문 캐시와 상세 검수 보고서는 저장소 밖에 보관한다.

Python 3와 `scripts/requirements-sync.txt`의 의존성을 준비한다.
원본 응답 캐시는 저장소 밖의 임시 경로를 사용한다.

```powershell
python scripts/import_naver_posts.py --since YYYY-MM-DD --cache <외부_캐시_경로> --dry-run
python scripts/import_naver_posts.py --since YYYY-MM-DD --cache <외부_캐시_경로>
python generate_blog_indexes.py --personal-only
python scripts/verify_personal_sync.py --cache <외부_캐시_경로> --baseline-ref <동기화_전_40자리_커밋_SHA> --update-sitemap
```

게시글 ID를 기준으로 이미 수입된 글은 건너뛴다. RSS의 날짜 범위가 부족하거나 같은 ID의 기존 파일과 목록이 충돌하면 중단한다. `python scripts/test_import_dedup.py`로 오프라인 회귀 검사를 실행한다.

원문이 이전 동기화 후 바뀐 경우, 기존 페이지를 덮어써서 맞추지 않는다. 검수기에 `--baseline-ref <동기화_전_40자리_커밋_SHA>`를 지정하면 기존 파일·메타데이터 보존 여부와 신규 글의 엄격한 원문 일치를 각각 검사하고, 기존 원문 차이가 있는 ID를 별도로 보고한다. 기본 모드는 모든 원문이 일치해야 통과한다.

RSS는 최근 50편까지만 제공하므로, 신규 글이 50편 이상인 기간에는 공개 글 목록을 별도로 대조해야 한다.
동기화 전에 기준 커밋과 기간을 확인하고, 실행 후 `data/naver-posts.json` 및 변경 파일을 검토한다.
GitHub 게시 전 원문 대조·로컬 링크·이미지·메타데이터 검사와 화면 확인을 수행한다.
스크립트는 Git 커밋이나 업로드를 자동으로 수행하지 않는다.
